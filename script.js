const canvas=document.getElementById('scene'),ctx=canvas.getContext('2d');let w,h,dpr,particles=[],mouse={x:innerWidth/2,y:innerHeight/2,tx:innerWidth/2,ty:innerHeight/2};function resize(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);particles=Array.from({length:Math.min(150,Math.floor(w/8))},()=>({x:Math.random()*w,y:Math.random()*h,z:.2+Math.random()*.8,r:.5+Math.random()*1.8,s:.15+Math.random()*.45,a:.15+Math.random()*.5}))}addEventListener('resize',resize);addEventListener('pointermove',e=>{mouse.tx=e.clientX;mouse.ty=e.clientY});resize();function frame(t){mouse.x+=(mouse.tx-mouse.x)*.035;mouse.y+=(mouse.ty-mouse.y)*.035;ctx.clearRect(0,0,w,h);const ox=(mouse.x-w/2)*.018,oy=(mouse.y-h/2)*.018;particles.forEach(p=>{p.y-=p.s;if(p.y<-10){p.y=h+10;p.x=Math.random()*w}const px=p.x+ox*p.z,py=p.y+oy*p.z;ctx.globalAlpha=p.a;ctx.beginPath();ctx.arc(px,py,p.r*p.z,0,Math.PI*2);ctx.fillStyle='#d8b56a';ctx.fill()});ctx.globalAlpha=1;requestAnimationFrame(frame)}requestAnimationFrame(frame);const loader=document.getElementById('loader');addEventListener('load',()=>setTimeout(()=>loader.classList.add('hide'),500));setTimeout(()=>loader.classList.add('hide'),1800);const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('visible')),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));addEventListener('scroll',()=>document.querySelector('.nav').classList.toggle('scrolled',scrollY>30),{passive:true});document.querySelectorAll('.tilt').forEach(card=>card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${-y*9}deg) rotateY(${x*11}deg) translateZ(8px)`}));document.querySelectorAll('.tilt').forEach(card=>card.addEventListener('pointerleave',()=>card.style.transform=''));const dot=document.querySelector('.cursor-dot'),ring=document.querySelector('.cursor-ring');function cursor(){dot.style.left=mouse.tx+'px';dot.style.top=mouse.ty+'px';ring.style.left=mouse.x+'px';ring.style.top=mouse.y+'px';requestAnimationFrame(cursor)}cursor();document.querySelectorAll('a,.tilt').forEach(el=>{el.addEventListener('pointerenter',()=>{ring.style.width='55px';ring.style.height='55px';ring.style.borderColor='#d8b56a'});el.addEventListener('pointerleave',()=>{ring.style.width='34px';ring.style.height='34px'})});document.querySelectorAll('.magnetic').forEach(el=>el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.12}px,${(e.clientY-r.top-r.height/2)*.12}px)`}));document.querySelectorAll('.magnetic').forEach(el=>el.addEventListener('pointerleave',()=>el.style.transform=''));